copy / clone external modules to this folder

example:
```
cd modules
git clone https://github.com/schirkan/dynamic-electron-react-module-example
```

and build all modules:
```
cd ..
npm run build-modules
```
