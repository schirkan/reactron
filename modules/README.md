copy / clone external modules to this folder or install them via npm

example:
```
cd modules
git clone https://github.com/schirkan/reactron-hello-world
```
or
```
cd modules
npm i --save https://github.com/schirkan/reactron-hello-world
```